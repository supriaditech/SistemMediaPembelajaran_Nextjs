/** @type {import('next').NextConfig} */
const nextConfig = {  
    images: {
        domains: ['localhost', 'https://www.youtube.com/embed/', 'https://play.google.com/'],
    },};

export default nextConfig;
